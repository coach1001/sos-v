// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "org.sos.kms"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
